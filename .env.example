# Em produção use a URL pública (https://seu-dominio) para OG, compartilhamento e canonical.
# Se o proxy não enviar X-Forwarded-Host, sem isso pode aparecer IP interno do container nas URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# API de dados: PostgreSQL exposto via PostgREST (não é obrigatório usar o produto Supabase).
# Prioridade da URL base: POSTGREST_URL → DB_API_URL → SUPABASE_URL (nomes SUPABASE_* são aliases).
# Ex.: PostgREST na raiz: http://postgrest:3000  |  Atrás de /rest/v1: https://seu-dominio/rest/v1
# POSTGREST_URL=http://localhost:3001
SUPABASE_URL=http://localhost:3001
# Chave anon do Supabase: JWT com 3 segmentos (eyJ… … …). Sem prefixo "Bearer ". Se aparecer PGRST301, não use chave sb_publishable_* — use a anon JWT em Settings → API.
SUPABASE_ANON_KEY=replace-with-your-anon-or-public-api-key
# Obrigatório no servidor para admin (sincronizar preço, PATCH, histórico): a anon só tem SELECT (RLS).
SUPABASE_SERVICE_ROLE_KEY=replace-with-your-service-api-key

# Conexão direta com Postgres (para tabelas sensíveis, ex.: mercadolivre_tokens)
# Formato: postgres://USER:PASSWORD@HOST:PORT/DB?sslmode=require
DATABASE_URL=postgres://user:pass@localhost:5432/postgres

# Alternativa com aliases (opcional)
# DB_API_URL=http://localhost:3001
# DB_ANON_KEY=replace-with-your-anon-or-public-api-key
# DB_SERVICE_ROLE_KEY=replace-with-your-service-api-key

# Admin cookie session (HS256) - mínimo 32 chars
ADMIN_JWT_SECRET=replace-with-random-64-char-secret
ADMIN_JWT_COOKIE_NAME=zuni_admin

# Cron: validação de links de afiliado em /api/cron/validate-affiliate-links
# Use o mesmo valor no curl: Authorization: Bearer <CRON_SECRET>
CRON_SECRET=replace-with-long-random-string

# App Flutter (worker): mesma chave no app (Configurações) e header Authorization: Bearer ...
# Gere uma string longa aleatória (mín. 16 caracteres no código).
# MOBILE_APP_API_KEY=replace-with-long-random-mobile-key

# Produção/container — não copie estas linhas para .env.local ao desenvolver com `next dev`
# (o Next.js define NODE_ENV ao arrancar).
# NODE_ENV=production
# NEXT_TELEMETRY_DISABLED=1

# Mercado Livre — Playwright (importação / sync no servidor)
# Perfil persistente com login ML (headless usa o mesmo perfil em Linux/Docker):
# ML_PLAYWRIGHT_USER_DATA_DIR=.playwright/ml-user-data
# Chromium do sistema (recomendado no servidor; mesma build em que fez login).
# Ubuntu Snap costuma ser /snap/bin/chromium; se /usr/bin/chromium não existir, o código tenta Snap automaticamente.
# ML_PLAYWRIGHT_EXECUTABLE_PATH=/snap/bin/chromium
# ou canal instalado: ML_PLAYWRIGHT_CHANNEL=chromium
# UA Linux automático em process.platform linux; override opcional:
# ML_PLAYWRIGHT_USER_AGENT=Mozilla/5.0 ...
# Desativar visita à home ML antes da PDP (só debug): ML_PLAYWRIGHT_SKIP_WARMUP=1
# Importação e sync reimportam com Chromium em modo gráfico (abre e fecha) se houver sessão X11 (DISPLAY) ou Wayland.
# Servidor com X11: se o processo Node não herdar DISPLAY (ex. systemd), defina o ecrã local:
# ML_PLAYWRIGHT_X11_DISPLAY=:1
# Com ML_PLAYWRIGHT_AUTO_X11=1 (defeito), se DISPLAY continuar vazio: DISPLAY=:1; XAUTHORITY: primeiro legível entre GDM (ex.: /run/user/1000/gdm/Xauthority), ~/.Xauthority.
# ML_PLAYWRIGHT_GRAPHICAL_UID=1000
# Home do utilizador com sessão X11 (cookie em ~/.Xauthority); útil quando o Node corre como root/PM2:
# ML_PLAYWRIGHT_GRAPHICAL_HOME=/home/diego
# ML_PLAYWRIGHT_X11_AUTHORITY_DEFAULT=/run/user/1000/gdm/Xauthority
# ML_PLAYWRIGHT_AUTO_X11=0  (desativar defaults em CI/Docker sem X)
# ML_PLAYWRIGHT_X11_DISPLAY_DEFAULT=:1
# ML_PLAYWRIGHT_X11_XAUTHORITY=/run/user/1000/gdm/Xauthority
# (só defina se o ficheiro existir e for legível pelo processo Node; senão omita — o código procura em /run/user/*/gdm/Xauthority e ~/.Xauthority)
# Cookie copiado (ex. sudo cp ~/.Xauthority /var/lib/zunistore/ml-xauth && chmod 644) quando o Node corre como root e não lê o home do utilizador gráfico:
# ML_PLAYWRIGHT_XAUTHORITY_FILE=/var/lib/zunistore/ml-xauth
# Forçar DISPLAY=:1 e XAUTHORITY GDM (Docker/PM2): ML_PLAYWRIGHT_X11_PIN_DEFAULTS=1
# Alias: ML_PLAYWRIGHT_DISPLAY (mesmo efeito que ML_PLAYWRIGHT_X11_DISPLAY)
# Arranque com X11 + xhost (root/PM2): npm run start:x11  ou  bash scripts/run.sh
# source scripts/ml-playwright-x11.sh   (alternativa)
# ML_PLAYWRIGHT_HEADLESS=1
# Em Linux sem DISPLAY/Wayland o modo interativo (janela) é desativado automaticamente — use headless + perfil logado.
# Com `next dev` em máquina com X11, por defeito pode abrir janela se o ML bloquear. Em servidor sem X11 nunca abre janela.
# ML_PLAYWRIGHT_HEADED_ON_BLOCK=0
# Chromium: sandbox desativado no código (root/Ubuntu); preferir deb: ML_PLAYWRIGHT_EXECUTABLE_PATH=/usr/bin/chromium
# Flags extra (separadas por espaço): ML_PLAYWRIGHT_EXTRA_CHROMIUM_ARGS=--disable-extensions
# Atraso (ms) antes de fechar o browser em modo gráfico; vazio = 250ms por defeito; 0 = sem espera
# ML_PLAYWRIGHT_HEADED_CLOSE_DELAY_MS=500
# ML_PLAYWRIGHT_HEADED_SKIP_CLOSE_DELAY=1  (sem atraso antes de fechar)
# Ms a aguardar após goto (hidratação ML); opcional
# ML_PLAYWRIGHT_POST_GOTO_MS=2000
# Tempo máximo (ms) a esperar login/captcha no modo interativo (mín. 10000)
# ML_PLAYWRIGHT_INTERACTIVE_TIMEOUT_MS=300000
